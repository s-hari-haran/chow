[build]
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "20"

# Send all actual page routes to the SSR function
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server"
  status = 200
